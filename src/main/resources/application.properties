# Use wildcard "*" to expose all endpoints
# Can also expose individual endpoints with comma-delimited list (health,info)
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

# exclude individual endpoints with comma-delimited list (health,info)
management.endpoints.web.exposure.exclude=health,info

info.app.name=Springboot 3 Demo
info.app.description=this is new version of springboot
info.app.version=1.0.0

# Define Custom Application Properties
trainer.name=Joahan
team.name=Dragon

# Change Springboot embedded server port
server.port=8085

# Set the Context Path of Application
#server.servlet.context-path=/api

# Set Lazy Initialization
#spring.main.lazy-initialization=true